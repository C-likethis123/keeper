{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "localhost",
	"description": "enables permissions for localhost HTTP serving (production builds)",
	"remote": {
		"urls": ["http://localhost:*"]
	},
	"windows": ["main"],
	"permissions": ["core:default", "dialog:allow-open"]
}
