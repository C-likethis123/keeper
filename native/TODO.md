## Phase 2: Math Blocks

1. LaTex is not rendered when i tried two libraries: react-native-katex and react-native-webview, that logseq purportedly uses.
2. In its current state we render a monospace display like a codeblock
3. The goal is to render Latex blocks, and it should work cross platform